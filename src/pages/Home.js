/**
 * Page d'accueil
 *
 * @param {HTMLElement} element
 * @returns {void}
 */
export const Home = (element) => {
  element.innerHTML = `
  <div id="homePage">
    <h1 class="Accueil">Accueil</h1>
  </div>

  `;
};
